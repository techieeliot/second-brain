import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack is enabled by the `dev` script. Next's default workspace
  // detection works with this app's `src/app` layout.
};

export default nextConfig;
